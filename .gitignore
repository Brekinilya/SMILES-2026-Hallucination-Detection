# Python cache
__pycache__/
*.py[cod]
*.pyo

# HuggingFace model cache
.cache/

# Virtual environments
.venv/
venv/
env/

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db

# Local hidden-state caches (regenerable via experiments/extract_cache*.py)
cache/features.npz
cache/features_v2.npz
cache/features_v3.npz
cache/extract*.log
solution_run*.log

# Experiment logs (kept .py sources, drop heavy logs)
experiments/results/*.log
experiments/results/*.npz

# Internal-only personal notes (don't commit)
JOURNAL.*

# Note: predictions.csv and results.json are deliverables and MUST be committed.
